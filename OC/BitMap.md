### BitMap
```
bitmap结构, 使用每一个bit代表一个不重复的数字(0代表存在1代表不存在),若存储N个数字,需要N个Bit (N/8)个字节

寻找N在第一个字节  N/8
寻找N在某一个字节中的位置 M/*

数字集合[1,4,6,8]

bit 8 7 6 5 4 3 2 1 
num 1 0 1 0 1 0 0 1


32位位图中 一个像素点占用32个bit(4个字节) RGBA 一个值占用一个字节
在Quzrtz中,Bitmap由CGImageRef封装

```
