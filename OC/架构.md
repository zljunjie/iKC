## 架构
```
http://www.jishudog.com/5724/html

MVC基础

模拟class的load方法
对应用到的模块进行注册,并收集模块指定接口内容，在不同的生命周期进行注册
管理被注册业务模块

MiddleManage
OpenUrl

模块 -> 业务与非业务
应用层 + 服务层 + 数据层
通用功能层 本地化 加密 网络 数据库 通用代码块功能
应用层 -> view-tree + viewContext + service + data

 
 交互式客户端  缩水版的动态化,预设响应指令
 命令式缓存 客户端启动后服务端下发文件缓存指令, 客户端获取信息后对文件进行缓存
 
 
 拆分
 1 保留重要的任务, 对非重要的任务进行拆分
 2 拆分后的模块尽量做到高可复用性
 3 尽可能提高拆分模块后的抽象度
 
 设计
 1 尽量少的使用继承
 2 规范代码结构
 3 Controller工作精简
 
 ```


MVC  

V+VCX++S+D
V+VC+S+D



1 进行代码规范
2 拆分不重要的
